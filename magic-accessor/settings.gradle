rootProject.name = 'magic-accessor'

include 'annotations'
include 'compiler'
include 'example'

dependencyResolutionManagement {
	repositories {
		mavenCentral()
		maven { url 'https://jitpack.io' }
	}
}
