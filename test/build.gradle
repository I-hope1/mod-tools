plugins {
    id 'java'
}

group = 'org.example'
version = '1.5.5'

sourceSets.main.java.srcDirs = ["src"]
sourceSets.test.java.srcDirs = ["src"]

dependencies {
//	def files = configurations.compileClasspath.asList() + configurations.runtimeClasspath.asList()
//	files.removeIf { it.name.contains("test") }
//	implementation (files.collect { it.absolutePath })
	println "parent: " + parent.project
	implementation files(parent.configurations.compileClasspath.asList().collect { it.absolutePath})
	implementation parent.project
	implementation files("lib/junit-4.13.1.jar", "lib/hamcrest-core-1.3.jar")
}